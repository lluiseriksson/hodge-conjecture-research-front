---
brick_id: NG080
status: NO-GO
base_field: C with rational chains
variety: an arbitrary class-specific B058 detector and actual isolated nodal collision total space
smoothness: nearby fibers smooth; target nodal
projectivity: ambient X and collision projective
dimension: ambient 2n; hyperplane fibers 2n-1; comparison classes degree 2n
codimension: middle codimension n
coefficient_field: Q
cohomology_theory: relative thimble complexes, relative singular homology, lift torsors, and primitive ambient realization
hodge_type: target relation and ambient class rational type (0,0) after Q(n)
cycle_class_map: CH^n(X)_Q -> H^(2n)(X,Q(n)); no cycle is constructed
cycle_equivalence: rational equivalence
scope: relative and fiberwise
dependencies: B104, G067
claim: To compare the selected B057 detector with a Saito relative cycle, one must construct a chain map from the entire distributed thimble complex to the entire nearby-fiber relative chain complex.
falsifier: B104's single class-specific relative bordism with the correct local boundary and compatible ambient realization
---

# NG080 — A full map on every thimble is unnecessary

**Status:** NO-GO

G067 requested a chain map
$C_{\mathrm{dist}}\to C_*(Y_c,Z_c)$ on the complete distributed complex.
B104 proves that the terminal class-specific argument only needs one lift of
the chosen local relation to be relatively bordant to the one selected
detector class in the collision total space.

The full map would be useful and sufficient, but it is not necessary. The
minimal re-entry condition is G068: construct the collision pair, define the
lift-independent obstruction coset for the specified detector, and prove it
vanishes with compatible ambient realization.
